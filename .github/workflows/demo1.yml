name: Events Demo 1 if workflow changed and push
on:
  pull_request:
    types:
      - opened
    branches:
      - main # main 
      - 'dev-*' # dev-new or dev-this-is-new
      - 'feat/**' # feat/new or feat/new/button
      # developer-1 would not trigger is not covered above
  workflow_dispatch:
  push:
    branches:
      - main # main 
      - 'dev-*' # dev-new or dev-this-is-new
      - 'feat/**' # feat/new or feat/new/button
      # developer-1 would not trigger is not covered above
    paths:
      - '.github/workflows/*' # do trigger if workflows changed
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output event data
        run: echo "${{ toJSON(github.event) }}"
      - name: Get code
        uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Test code
        run: npm run test
      - name: Build code
        run: npm run build
      - name: Deploy project
        run: echo "Deploying..."